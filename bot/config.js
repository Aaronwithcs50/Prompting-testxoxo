require('dotenv').config(); const required=['DISCORD_TOKEN','CLIENT_ID','MONGODB_URI']; for(const k of required){ if(!process.env[k]) throw new Error(`${k} is required`); } module.exports={ token:process.env.DISCORD_TOKEN, clientId:process.env.CLIENT_ID, ownerIds:(process.env.OWNER_IDS||'').split(',').map(s=>s.trim()).filter(Boolean), defaultPrefix:process.env.DEFAULT_PREFIX||'!' };