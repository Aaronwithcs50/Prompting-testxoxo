const {cmd,simple}=require('../_factory'); module.exports=cmd('clearwarnings','clearwarnings command','moderation',async(i)=>simple(i,'clearwarnings','The clearwarnings command is available and wired for slash and prefix usage.'));