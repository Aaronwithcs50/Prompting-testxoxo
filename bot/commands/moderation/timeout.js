const {cmd,simple}=require('../_factory'); module.exports=cmd('timeout','timeout command','moderation',async(i)=>simple(i,'timeout','The timeout command is available and wired for slash and prefix usage.'));