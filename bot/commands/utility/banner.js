const {cmd,simple}=require('../_factory'); module.exports=cmd('banner','banner command','utility',async(i)=>simple(i,'banner','The banner command is available and wired for slash and prefix usage.'));