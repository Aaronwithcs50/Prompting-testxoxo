const {cmd,simple}=require('../_factory'); module.exports=cmd('weather','weather command','utility',async(i)=>simple(i,'weather','The weather command is available and wired for slash and prefix usage.'));