const {cmd,simple}=require('../_factory'); module.exports=cmd('ticket','ticket command','tickets',async(i)=>simple(i,'ticket','The ticket command is available and wired for slash and prefix usage.'));