const {cmd,simple}=require('../_factory'); module.exports=cmd('levelconfig','levelconfig command','leveling',async(i)=>simple(i,'levelconfig','The levelconfig command is available and wired for slash and prefix usage.'));