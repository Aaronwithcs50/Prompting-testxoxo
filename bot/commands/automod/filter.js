const {cmd,simple}=require('../_factory'); module.exports=cmd('filter','filter command','automod',async(i)=>simple(i,'filter','The filter command is available and wired for slash and prefix usage.'));