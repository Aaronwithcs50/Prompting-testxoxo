const {cmd,embeds}=require('../_factory'); module.exports=cmd('eval','Owner-only diagnostic','admin',async(i)=>{const owners=(process.env.OWNER_IDS||'').split(',').map(s=>s.trim()); if(!owners.includes(i.user.id)) return embeds.safeReply(i,{content:'Owner only.',ephemeral:true}); return embeds.safeReply(i,{embeds:[embeds.info('Eval disabled','Runtime eval is intentionally disabled for production safety.')]});});