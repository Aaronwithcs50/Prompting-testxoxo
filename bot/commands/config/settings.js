const {cmd,simple}=require('../_factory'); module.exports=cmd('settings','settings command','config',async(i)=>simple(i,'settings','The settings command is available and wired for slash and prefix usage.'));