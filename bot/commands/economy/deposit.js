const {cmd,simple}=require('../_factory'); module.exports=cmd('deposit','deposit command','economy',async(i)=>simple(i,'deposit','The deposit command is available and wired for slash and prefix usage.'));