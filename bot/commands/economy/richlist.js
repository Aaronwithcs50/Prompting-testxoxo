const {cmd,simple}=require('../_factory'); module.exports=cmd('richlist','richlist command','economy',async(i)=>simple(i,'richlist','The richlist command is available and wired for slash and prefix usage.'));