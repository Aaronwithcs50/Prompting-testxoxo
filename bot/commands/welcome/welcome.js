const {cmd,simple}=require('../_factory'); module.exports=cmd('welcome','welcome command','welcome',async(i)=>simple(i,'welcome','The welcome command is available and wired for slash and prefix usage.'));