const fs=require('fs'); const path=require('path'); const locales={}; for(const f of ['en','es','fr','de','pt']) locales[f]=JSON.parse(fs.readFileSync(path.join(__dirname,'..','locales',`${f}.json`),'utf8')); function t(lang,key,vars={}){let s=(locales[lang]||locales.en)[key]||locales.en[key]||key; for(const [k,v] of Object.entries(vars)) s=s.replaceAll(`{${k}}`,v); return s;} module.exports={t};