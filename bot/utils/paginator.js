const { ActionRowBuilder, ButtonBuilder, ButtonStyle } = require('discord.js'); async function paginate(interaction, pages, ephemeral=false){let page=0; const row=()=>new ActionRowBuilder().addComponents(new ButtonBuilder().setCustomId('prev').setLabel('Previous').setStyle(ButtonStyle.Secondary).setDisabled(page===0),new ButtonBuilder().setCustomId('next').setLabel('Next').setStyle(ButtonStyle.Secondary).setDisabled(page===pages.length-1)); const msg=await interaction.reply({embeds:[pages[page]],components:[row()],ephemeral,fetchReply:true}); const col=msg.createMessageComponentCollector({time:60000}); col.on('collect',async i=>{if(i.user.id!==interaction.user.id)return i.reply({content:'This paginator is not yours.',ephemeral:true}); page += i.customId==='next'?1:-1; await i.update({embeds:[pages[page]],components:[row()]});}); col.on('end',()=>msg.edit({components:[]}).catch(()=>{}));} module.exports={paginate};