const { createCanvas, loadImage } = require('@napi-rs/canvas'); const { AttachmentBuilder } = require('discord.js'); async function rankCard(user, data){ const c=createCanvas(934,280),x=c.getContext('2d'); const g=x.createLinearGradient(0,0,934,280); g.addColorStop(0,'#0D0F17'); g.addColorStop(1,'#5865F2'); x.fillStyle=g; x.fillRect(0,0,934,280); x.fillStyle='#fff'; x.font='bold 42px sans-serif'; x.fillText(user.username,250,95); x.font='28px sans-serif'; x.fillText(`Level ${data.level} • Rank #${data.rank}`,250,145); x.fillStyle='rgba(255,255,255,.2)'; x.fillRect(250,180,600,28); x.fillStyle='#57F287'; x.fillRect(250,180,600*Math.max(0,Math.min(1,data.progress||0)),28); try{const img=await loadImage(user.displayAvatarURL({extension:'png',size:256})); x.save(); x.beginPath(); x.arc(125,140,82,0,Math.PI*2); x.clip(); x.drawImage(img,43,58,164,164); x.restore();}catch{} return new AttachmentBuilder(await c.encode('png'),{name:'rank.png'});} module.exports={rankCard};