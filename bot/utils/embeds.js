const { EmbedBuilder } = require('discord.js'); const { COLORS } = require('../../shared/constants'); function base(title, description, color) { return new EmbedBuilder().setColor(color).setTitle(title).setDescription(description || null).setTimestamp(); } module.exports = { success: (t,d)=>base(t,d,COLORS.success), error: (t,d)=>base(t,d,COLORS.danger), warn: (t,d)=>base(t,d,COLORS.warning), info: (t,d)=>base(t,d,COLORS.primary), log: (t,d,c=COLORS.info)=>base(t,d,c), safeReply: async (i,o)=> (i.deferred||i.replied ? i.followUp(o) : i.reply(o)) };