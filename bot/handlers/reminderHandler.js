const Reminder=require('../../models/Reminder'); function startReminderHandler(client){setInterval(async()=>{const due=await Reminder.find({completed:false,remindAt:{$lte:new Date()}}).lean(); for(const r of due){await Reminder.updateOne({_id:r._id},{$set:{completed:true}}); const ch=client.channels.cache.get(r.channelId); ch?.send({content:`<@${r.userId}> reminder: ${r.message}`}).catch(()=>{});}},30000).unref?.();} module.exports={startReminderHandler};