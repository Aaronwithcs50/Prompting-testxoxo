const { Client, GatewayIntentBits, Partials, Collection } = require('discord.js'); const config=require('./config'); const connectDb=require('./utils/db'); const {loadCommands}=require('./handlers/commandHandler'); const {loadEvents}=require('./handlers/eventHandler'); const {startGiveawayHandler}=require('./handlers/giveawayHandler'); const {startReminderHandler}=require('./handlers/reminderHandler'); const logger=require('./utils/logger'); async function main(){ await connectDb(); const client=new Client({intents:[GatewayIntentBits.Guilds,GatewayIntentBits.GuildMembers,GatewayIntentBits.GuildMessages,GatewayIntentBits.GuildMessageReactions,GatewayIntentBits.GuildVoiceStates,GatewayIntentBits.MessageContent],partials:[Partials.Message,Partials.Channel,Partials.Reaction]}); client.commands=new Collection(); loadCommands(client); loadEvents(client); startGiveawayHandler(client); startReminderHandler(client); await client.login(config.token);} main().catch(e=>{logger.error(e); process.exit(1);});