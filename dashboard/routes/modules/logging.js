const express=require('express'); const router=express.Router({mergeParams:true}); router.get('/',(req,res)=>res.render('guild/logging',{title:'Logging',user:req.session.user,guild:req.guild,csrfToken:req.csrfToken()})); module.exports=router;