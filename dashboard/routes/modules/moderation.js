const express=require('express'); const router=express.Router({mergeParams:true}); router.get('/',(req,res)=>res.render('guild/moderation',{title:'Moderation',user:req.session.user,guild:req.guild,csrfToken:req.csrfToken()})); module.exports=router;