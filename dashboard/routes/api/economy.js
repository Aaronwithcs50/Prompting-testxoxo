const express=require('express'); const Joi=require('joi'); const Guild=require('../../../models/Guild'); const {invalidateGuild}=require('../../../bot/utils/cache'); const router=express.Router({mergeParams:true}); router.get('/',async(req,res,next)=>{try{const guild=await Guild.findOneAndUpdate({guildId:req.params.id||req.params.guildId},{guildId:req.params.id||req.params.guildId},{upsert:true,new:true,setDefaultsOnInsert:true}).lean(); res.json({ok:true,guild});}catch(e){next(e);}}); router.post('/',async(req,res,next)=>{try{const schema=Joi.object().unknown(true); const value=await schema.validateAsync(req.body); const guildId=req.params.id||req.params.guildId; const guild=await Guild.findOneAndUpdate({guildId},{$set:value},{new:true,upsert:true,setDefaultsOnInsert:true}).lean(); invalidateGuild(guildId); res.json({ok:true,guild});}catch(e){next(e);}}); module.exports=router;