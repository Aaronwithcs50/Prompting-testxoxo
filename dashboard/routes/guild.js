const express=require('express'); const AuditLog=require('../../models/AuditLog'); const router=express.Router({mergeParams:true}); router.get('/dashboard/:id',async(req,res,next)=>{try{const logs=await AuditLog.find({guildId:req.params.id}).sort({createdAt:-1}).limit(10).lean(); res.render('guild/overview',{title:'Overview',user:req.session.user,guild:req.guild,logs,csrfToken:req.csrfToken()});}catch(e){next(e);}}); module.exports=router;